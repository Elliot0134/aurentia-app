import React from 'react';
import { CollaborationTester } from '../components/CollaborationTester';

const TestCollaboration: React.FC = () => {
  return (
    <div className="min-h-screen bg-gray-50">
      <CollaborationTester />
    </div>
  );
};

export default TestCollaboration;